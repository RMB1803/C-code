#include <stdio.h>

int main() {
    char name[30]; // initializing the string.
    printf("Enter name: ");
    // scanf("%s", name);
    // printf("%s", name);
    //scanf("%5s", name); // reads only first 5 characters from the input.
    //gets(name); // reads the whole input.
    //printf("%.5s\n", name); // only 5 characters.
    //printf("%10.5s\n", name); // printing 5 characters in a field width of 10.
    //printf("%s", &name[2]); // starts printing from the specified index till the end.
    //puts(name); // prints entire string and automatically adds new line.
}